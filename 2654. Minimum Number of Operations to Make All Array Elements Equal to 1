You are given a 0-indexed array nums consisiting of positive integers. You can do the following operation on the array any number of times:

Select an index i such that 0 <= i < n - 1 and replace either of nums[i] or nums[i+1] with their gcd value.
Return the minimum number of operations to make all elements of nums equal to 1. If it is impossible, return -1.

The gcd of two integers is the greatest common divisor of the two integers.

 

Example 1:

Input: nums = [2,6,3,4]
Output: 4
Explanation: We can do the following operations:
- Choose index i = 2 and replace nums[2] with gcd(3,4) = 1. Now we have nums = [2,6,1,4].
- Choose index i = 1 and replace nums[1] with gcd(6,1) = 1. Now we have nums = [2,1,1,4].
- Choose index i = 0 and replace nums[0] with gcd(2,1) = 1. Now we have nums = [1,1,1,4].
- Choose index i = 2 and replace nums[3] with gcd(1,4) = 1. Now we have nums = [1,1,1,1].
Example 2:

Input: nums = [2,10,6,14]
Output: -1
Explanation: It can be shown that it is impossible to make all the elements equal to 1.
 

Constraints:

2 <= nums.length <= 50
1 <= nums[i] <= 106


----------------------------------------------------

class Solution {
    private int gcd(int a, int b) {
        if (b == 0) return a;
        return gcd(b, a % b);
    }

    public int minOperations(int[] nums) {
        final int n = nums.length;
        int cntOne = 0, allGcd = 0; // count of 1s, and gcd of entire array
        for (int x : nums) {
            if (x == 1) ++cntOne;
            allGcd = gcd(allGcd, x);
        }

        // Case 1: array already contains 1
        // each non-1 can be turned into 1 by one gcd operation with an adjacent 1
        if (cntOne > 0) return n - cntOne;

        // Case 2: gcd of entire array > 1 → impossible to create any 1
        if (allGcd > 1) return -1;

        // Case 3: no 1 exists, but possible to create one
        // find the shortest subarray whose gcd is 1
        int minOp = n;
        for (int i = 0; i < n; ++i) {
            int cGcd = 0;
            for (int j = i; j < n; ++j) {
                cGcd = gcd(cGcd, nums[j]);
                if (cGcd == 1) {
                    if (j - i + 1 < minOp) minOp = j - i + 1;
                    break;
                }
            }
        }

        // (minOp - 1): steps needed to create the first 1
        // (n - 1): once the first 1 appears, each of the remaining (n - 1) non-1 elements
        //          can become 1 by one gcd operation with an adjacent 1
        return minOp + n - 2;
    }
}
