A k x k magic square is a k x k grid filled with integers such that every row sum, every column sum, and both diagonal sums are all equal. The integers in the magic square do not have to be distinct. Every 1 x 1 grid is trivially a magic square.

Given an m x n integer grid, return the size (i.e., the side length k) of the largest magic square that can be found within this grid.

 

Example 1:


Input: grid = [[7,1,4,5,6],[2,5,1,6,4],[1,5,4,3,2],[1,2,7,3,4]]
Output: 3
Explanation: The largest magic square has a size of 3.
Every row sum, column sum, and diagonal sum of this magic square is equal to 12.
- Row sums: 5+1+6 = 5+4+3 = 2+7+3 = 12
- Column sums: 5+5+2 = 1+4+7 = 6+3+3 = 12
- Diagonal sums: 5+4+3 = 6+4+2 = 12
Example 2:


Input: grid = [[5,1,3,1],[9,3,3,1],[1,3,3,8]]
Output: 2
 

Constraints:

m == grid.length
n == grid[i].length
1 <= m, n <= 50
1 <= grid[i][j] <= 106

------------------------------------



class Solution {
    private boolean isValid(int[][] grid, int r, int c, int k){
        int target = 0;

        //calc target sum using first row 
        for(int j=c; j<c+k ;j++){
            target +=grid[r][j];
        }

        
        for(int i=r; i<r+k;i++){
           int rowsum = 0;
           for(int j =c;j< c+k; j++){
                rowsum += grid[i][j];
           }
           if(rowsum != target) return false;
        }

        
        for(int i=c;i<c+k;i++){
            int colsum = 0;
            for(int j=r;j<r+k;j++){
                colsum += grid[j][i];
            }
            if(colsum != target) return false;
        }

        //check both diagonal sum
        int diag1 =0, diag2 =0;
        for(int i=0;i<k;i++){
            diag1 += grid[r+i][c+i];
            diag2 += grid[r+i][c+k-1-i];
        }

        return diag1==target && diag2==target;
    }
    public int largestMagicSquare(int[][] grid) {
        int row = grid.length;
        int col = grid[0].length;

        //generate all the squares using top-left grid index
        for(int k = Math.min(row,col); k>=1;k--){
            for(int i=0;i<=row-k; i++){
                for(int j =0;j<=col-k;j++){
                    if(isValid(grid,i,j,k)) return k;
                }
            }
        }
        return 1;

    }
}
